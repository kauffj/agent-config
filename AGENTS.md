# Codex guidance for this repository

Global working preferences are already loaded through `~/.codex/AGENTS.md`,
which points to this repository's `CLAUDE.md`; do not duplicate them here.

This checkout is the live source behind `~/.claude` and the shared Codex skill
links. Preserve that symlink-based layout, leave ignored runtime/session state
alone, and keep shared instructions and skills portable across both agents.
