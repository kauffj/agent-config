- 2026-07-21 — `pkill -f <pattern>` matches the running shell command itself (the pattern text is in its own argv) and sends SIGTERM to the scripts process group (exit 144, cleanup lines skipped). Use `pkill -x <exact-proc-name>` for a known binary; never pattern-match on a string that also appears in your command. Verify a kill with `pgrep -x`, and dont trust `pgrep -af <pat>` output that includes your own invocation.
- 2026-07-30 — `pgrep -x`/`pkill -x` silently match nothing when the name exceeds 15 chars (kernel comm truncation, e.g. `gnome-text-editor` → `gnome-text-edit`). Use the truncated name with `-x`, and beware `-f`: the pattern may appear in your own shell's argv.
- 2026-07-30 — Do not anchor a performance diagnosis on the suspected component: establish a contemporaneous A/B and trace the event trigger before optimizing code. Here tab rendering was expensive but Claude's animated OSC title was what repeatedly invoked it.
- 2026-08-19 — A quota/rate "percentage" is meaningless without its reset time. I built the dual-account balancer scoring on raw session%/weekly% and used resets_at only for stale-snapshot decay, so 60% spent with 20 minutes to refill scored worse than 40% that had to last four hours — exactly backwards. Normalize any consumable against the time until it replenishes (capacity left ÷ capacity the window returns in the time remaining) before comparing two of them. Corollary: keep the *guards* on raw values — "can this run right now" is a different question from "which is cheaper to spend".
- 2026-08-19 — When the user says "you're smart, you probably got this right", check the code before agreeing. The flattering read cost nothing to verify (`grep -n "def score"`) and was wrong.
- 2026-08-23 — A Lua file-scope `local` is invisible to any closure defined ABOVE its declaration: the closure compiles the name as a global read and gets `nil` at runtime. I added `DELEGATE_STALE_S` next to the gradient constants but used it in the `update-status` handler ~120 lines earlier, which would have thrown "attempt to compare nil with number" inside a non-pcall'd loop and frozen the whole tab bar. Loading a config only PARSES it — `wezterm --config-file X ls-fonts` exited 0 on the broken version. Verifying a callback means exercising the callback (wezterm embeds Lua: a scratch config whose top-level code calls the closure under pcall proves it), and when adding a constant, declare it above its first use, not beside its thematic neighbors.
- 2026-08-23 — `git hash-object -w <file>` skips the repo's clean filters; only `--path <dest>` runs them. Staging settings.json that way put 9 lines of the auto-mode infra dossier into the index — the exact leak `filter.strip-automode` exists to prevent. When hand-building an index entry, pass `--path`, then verify with `git show :<file>` before committing.
- 2026-08-23 — A technically clean or recently changed repository is not automatically a good migration pilot: both `aristillus.net` and `fsp-mobile` were obsolete and should have been retired, not modernized. Git recency is weak evidence of relevance. Prefer a candidate whose current production use is explicitly documented or user-confirmed, then check that it is clean, reversible, and low-risk.
- 2026-08-24 — Do not silently expand “model-agnostic usage” into cross-harness session portability. Separate three requirements up front: shared durable project state, persistent sessions within each native client, and transferring a live session between clients. The first two need only standards-first files and thin adapters; the third is a different and much larger problem.
- 2026-08-24 — Jeremy does not review PRs or return to parked feature branches. Once a planned change has passed its review and verification gates, merge it directly to the repository's actual default branch, push it, monitor CI/deploy to completion, and remove the merged branch/worktree. Do not offer a PR or an unmerged branch as a completion path.
- 2026-08-27 — “Commit and push” authorizes delivery only once the requested result is actually complete. If inspection exposes an unfinished or narrower implementation, preserve it, catch the branch up safely, and re-establish an approved completion target before creating a new commit; never turn partial work into a commit merely because delivery was requested earlier.
- 2026-08-27 — Never recursively search a Codex or Claude home just to locate configuration: session/transcript trees are huge and may contain sensitive history, and a shallow glob like `!sessions/**` does not exclude a nested `.codex/sessions/` tree. Name the expected config files directly; if broad discovery is truly necessary, prune exact transcript/cache directories with `find -prune` or `rg --glob '!**/sessions/**'` before searching.
- 2026-08-27 — When opening an interactive terminal for the user, verify the exact pane is still alive and visibly foregrounded instead of assuming pane activation raised its containing window. Prefer a dedicated window, confirm its live text, and only then ask the user to interact.
- 2026-08-27 — A fleet launcher that resumes one agent and then drops into `exec bash` must scrub the launching session's account and harness identity before both operations. Otherwise a tab spawned from an alternate Claude account can pin the returned shell's `CLAUDE_CONFIG_DIR`, and the next `claude-acct` silently bypasses round-robin selection. Verify every launcher with a contaminated parent environment, a cross-vendor resume, and a second account-routed launch from the resulting shell.
- 2026-08-27 — A terminal launcher is not correct merely because the agent command exits cleanly: its user-facing contract includes returning to a usable login shell, with the tab closing only on a second exit. Exercise that boundary in a real pseudo-terminal or mux pane after changing process groups, TTY ownership, signals, or wrapper exit-status handling.
- 2026-08-28 — When asked how to change local agent configuration, rule 8 requires preparing the durable config, migration, documentation, and verification automatically wherever the sandbox permits. Leave the user only the irreducible boundary (such as restarting with a broader writable scope); do not turn a blocked final write into a manual procedure for the whole task.
- 2026-08-28 — A harness-specific workflow file is not automatically migration work. Before porting a command or hook, establish that it is still used; obsolete automation should be deleted instead of preserved in a new abstraction. Here `party-ops/.claude/commands/pop.md` was unused and should go, not become a shared skill.
- 2026-08-28 — Do not hand off a live Codex thread with `codex resume`: the original process still owns the active-writer lease, and session reuse restores its old cwd instead of gaining an intended broader workspace. Use `codex fork` only for a concurrent handoff that should retain the original workspace; start a fresh session for a new root, or prove the original writer exited before resuming.
- 2026-08-28 — A green Git-access check at a non-Git umbrella proves nothing about its child repositories. When a launcher claims fleet-wide access from `~/projects`, probe real descendant Git directories; make each eligible repository an effective runtime workspace root, and verify local opt-outs still keep their metadata read-only.
- 2026-08-28 — When a managed permission-profile schema changes, upgrade the installed profile before asking the user to restart into it. A launcher that recognizes only the new shape must fail closed on the old one, so a restart without the migration cannot possibly grant the intended access.
- 2026-08-29 — Codex CLI 0.150.1 can restore a forked thread's original cwd and sandbox even when `fork -C <new-root>` is supplied. Never use resume or fork to widen a session's workspace: start a fresh thread with the process cwd already at the intended root, then prove the effective boundary with actual descendant Git-directory write probes.
- 2026-08-30 — A successful managed-profile install and config check do not prove that a launched Codex session received writable roots. The launcher can still fail closed during workspace discovery; inspect the effective permission profile and exercise a reversible write inside real descendant `.git` directories before declaring a fleet launch usable.
- 2026-08-30 — Never invent a recovery subcommand from the intended semantics. Before asking the user to run any CLI command, execute its `--help`, verify the exact invocation against the installed version, and prefer performing the recovery automatically when the current sandbox still permits it.
- 2026-08-30 — Python's `Path.iterdir()` is lazy: filesystem errors can be raised while consuming the iterator, not when constructing it. When permission or disappearance is expected (especially under `/proc`), materialize the iterator inside the guarded `try` or wrap the iteration itself; constructing it in `try` and iterating afterward does not handle the error.
- 2026-08-30 — `git worktree repair` can leave a linked worktree's raw `.git` pointer unchanged when an old absolute path still resolves through a compatibility symlink. A relocation gate must inspect and canonicalize both sides of the linkage—the main repository's `worktrees/*/gitdir` and the linked checkout's `.git` text—rather than treating Git command success as proof that the old prefix is gone.
- 2026-08-30 — An absolute path to a CLI does not determine that CLI's project root. `workspace.mjs` deliberately roots runtime state at `process.cwd()`, so invoking it from `~/projects` created an empty umbrella `.workspaces` and made the relocation check fail. For any stateful project CLI, set the intended cwd explicitly and verify that no state appeared at the caller's cwd.
- 2026-08-30 — Renaming a Claude project requires both a native trust entry for the canonical cwd and migration of Claude's path-encoded `~/.claude/projects/<cwd>` session directory. Do not rewrite the whole live `~/.claude.json` while other agents may be using Claude's own state writer: let Claude accept the new path through its native trust UI, retain the old trusted entry, preserve the old encoded directory as a compatibility link, and verify resume from the canonical cwd.
- 2026-08-30 — A relocation inventory that searches only the old absolute checkout prefix misses branded path fragments in structured runtime state. Inspect every path-bearing field semantically (for example `screenshotDir`), migrate only the known fields, and leave historical prose containing the old name unchanged.
- 2026-08-30 — Never compare `realpath` output to an uncanonicalized path beneath a symlinked parent. Here `~/.claude` itself is a support link, so the migrated Claude session link and target resolved to the same directory while their canonical and logical strings differed; canonicalize both sides before asserting identity and exercise the assertion against the installed link layout.
- 2026-08-30 — Visual text in a full-screen TUI may not exist as literal PTY bytes: Claude's normal renderer drew spaces with cursor movement, so `Yes, I trust this folder` was readable on screen but an exact byte trigger could never match it. For automated interactive gates, use the tool's accessibility/screen-reader mode when available, match its stable input prompt, send an explicit choice rather than navigation keys, and validate the real raw PTY stream before handoff.
- 2026-08-30 — When a user says a migration or cutover script ran while the session was active, stop and re-inventory the checkout and runtime links before attributing dirty files or continuing edits. Concurrent scripted state is an input to reconcile, not unexplained user drift.
- 2026-08-30 — Overlapping `sed` ranges can print the boundary line twice and create a fake duplicate-code finding. Before logging or editing a suspected duplicate, verify it with an exact `rg -n` or one non-overlapping view.
